# CLAUDE.md - כללי עבודה לריפו tironot-landing

> קובץ זה נקרא אוטומטית בתחילת כל שיחה בריפו הזה.

## מה יש כאן

דפי הנחיתה הסטטיים של טירונות סוכנים, מוגשים ב-GitHub Pages תחת דומיין מותאם (`CNAME` ← `agent-course.aimprove.co.il`). כלומר האתר יושב ב**שורש** הדומיין, לא תחת תת-נתיב.

דפים: `index.html` (דף נועם), `stories.html`, `faq/`, `bogrim/` (דף הבוגרים), `referral-program/` (תוכנית הממליצים). הנכסים המשותפים (לוגו, מפות מסע, תמונות בוגרים) יושבים בשורש ובתיקיות `students/` ו-`bogrim/images/`.

## איפה הכללים המחייבים

1. **הכללים הגלובליים** - `~/.claude/CLAUDE.md`. ⚠️ הקובץ הזה **user-level ולא בגיט**, ולכן קיים בנפרד על כל אחד מ-3 המקים ועלול להיות לא מעודכן.
2. **כללים גלובליים שמעוגנים בגיט** - `~/קוד/Storyteling/CLAUDE.md`. שם חיים הכללים שחייבים לרוץ על כל שלושת המקים, ולכן **הוא הנוסח המחייב** כשיש הבדל.

## הכלל שהכי רלוונטי לריפו הזה

**כלל 12 ב-`~/קוד/Storyteling/CLAUDE.md` - "דף שעולה לבן: הנכסים ב-404, ואותה בנייה מוגשת בשני מקומות". לקרוא שם את הנוסח המלא לפני דיבאג של דף שלא עולה, ולפני הוספת יעד פרסום שני לדף קיים.**

תמצית, וההשלכה הישירה כאן:

- דף שמחזיר 200 ועולה לבן אינו שרת שבור - הנכסים שלו חוזרים 404. בדיקה של הכתובת עצמה מחזירה 200 ומרגיעה לשווא; מה שמכריע הוא בדיקה של **נתיב נכס מתוך ה-HTML**.
- השורש החוזר: אותו תוצר מוגש בשני מיקומים. שורש דומיין ותת-נתיב הם **שני מיקומים שונים**, וכל נתיב מוחלט מקבע אחד ושובר את השני בשקט.
- **בריפו הזה הנתיבים כבר יחסיים** (`src="roadmap-desktop.png"`, `src="students/anat.jpg"`) - כלומר הוא במצב הבטוח. **לא להחליף אותם בנתיבים שמתחילים ב-`/`**, וגם לא להוסיף `<base href="...">` מוחלט. זה מה שיישבר ביום שדף מכאן ייבדק בתצוגה מקדימה תחת תת-נתיב, או יועתק לריפו אחר.
- אם דף מכאן מקבל יעד פרסום נוסף (Render/Vercel לצד GitHub Pages) - לאמת אותו **בשני** המיקומים לפני שמכריזים "עובד", לא רק בזה שנפגע.

> נוסף 16/08/2026 - בעקבות `monster-skills-landing` בריפו Storyteling, שהוגש דף לבן לחלוטין ב-Render מיום הקמתו: הבנייה הייתה מקובעת לתת-הנתיב של GitHub Pages בזמן ש-Render מגיש בשורש, ולכן כל JS/CSS/מודל GLB חזר 404 בעוד השרת עונה 200.
